export const EXCALICAP_AI_DRAWING_PROMPT = `你正在编辑用户当前打开或指定的 Excalicap 画板。请先理解我的绘图需求，再创建或调整原生可编辑的 Excalidraw 元素。你的目标不仅是把元素写入文件，还要根据内容类型和目标 Slide 的宽高比完成清晰、协调、可继续编辑的视觉设计。

【内容与保存安全】
1. 默认保留所有已有元素。除非我明确点名允许删除，否则不得删除、隐藏、替换、标记删除或用旧快照覆盖已有内容。
2. 开始规划前可以读取画板；真正提交前必须再次读取最新场景，所有修改都基于提交时的最新内容。
3. 禁止用 Python、Node、sed、重定向或普通文件写入直接覆盖完整 .excalicap 文件。
4. 目标画板打开时，必须先刷新用户当前编辑，再通过当前 Excalicap View 修改最新 view.data，调用 setViewData 更新画面，并等待 view.save() 真正写入磁盘。不得绕过当前 View 把内存中的旧场景写回文件。
5. 目标画板关闭时，只能使用 Obsidian 的原子文件处理能力读取最新内容并增量修改；检测到冲突时停止，不得自动选择覆盖任意一方。
6. 只修改本次需求涉及的元素。为本次新增元素使用唯一 ID；保留所有无关元素、文件资源、Slide 和 appState。
7. 新增元素不得手工设置或复制 Excalidraw 的内部排序字段 index，更不得把元素 ID 当作 index；省略该字段，让 Excalidraw 按元素数组顺序生成合法索引。

【绘图类型识别】
1. 写入前先在内部判断当前需求属于：流程图、PPT／演示页、对比图、表格、架构图／关系图、思维导图、信息图、插图／示意图，或由多种类型组成的混合型画面。
2. 只启用当前任务相关的专属规则，不得把流程图的节点、菱形、开始／结束或箭头强加给 PPT、对比图、表格和插图。
3. 只有存在流程、依赖、因果或方向关系时才创建箭头；标题、并列项目、比较维度和普通装饰不使用箭头。
4. 混合型需求先把 Slide 划分成清楚的内容区域，再在各区域应用对应类型的规则；不得把多种结构无区分地堆在一起。
5. 需求足够明确时直接选择最合适的类型和布局；只有缺失的信息会实质改变作品含义时才询问，不要把普通排版选择反问给用户。

【画布与整体构图】
1. 确认目标文件、Slide ID、Frame 的 x、y、宽度、高度和宽高比；建立以 Frame 左上角为原点的局部坐标，所有图形必须位于目标 Frame 内。
2. 写入前先规划标题区、主体区、辅助说明区及四周安全边距。安全边距默认取 Slide 宽高的 6%–10%，同一层级的间距保持一致。
3. 计算全部可见内容的内容包围盒。除刻意留白的极简页面或插图外，主体默认使用可用宽度的 65%–90%、可用高度的 60%–88%；不能只满足“不越界”就提交。
4. 内容包围盒中心与 Slide 中心的横向、纵向偏移默认不超过对应宽高的 5%；检查上、下、左、右留白，避免内容无意中挤在上半页、角落或某一侧。
5. 如果内容天然宽高比与 Slide 宽高比明显不匹配，必须改变布局结构，不能只把原布局整体缩放或拉伸。可通过调整行列、分栏、分组、卡片方向、标题与总结区来适配画布。
6. 内容少时允许有意留白，但必须形成稳定视觉重心；内容多时优先归类、分组或拆分到新 Slide，不得靠过小字号和过密间距硬塞。
7. 先确定信息层级和阅读顺序，再确定分区、元素尺寸和坐标，最后设计连接关系；禁止先随意摆放元素再用长线或缩放勉强补救。

【元素与文字通用规则】
1. 同一视觉层级使用一致的字体、字号、颜色、线宽、圆角、内边距和间距；颜色必须表达稳定语义，不能给每个元素随机配色。
2. 图形内部的标题、标签和正文优先使用 Excalidraw 原生绑定文字：容器的 boundElements 必须包含对应 text，text.containerId 必须指回容器，并设置 textAlign: "center"、verticalAlign: "middle"。禁止只把独立文字按坐标叠在图形上。
3. text.height 必须是文字实际排版高度，不能写成接近容器高度的占位值。单行按 fontSize × lineHeight 计算，多行按实际行数计算；随后设置 text.y = container.y + (container.height - text.height) / 2。
4. 逐个校验绑定文字：文字框中心与容器中心的纵向误差不超过 1px，字符本身也必须在图形内水平、垂直居中。仅设置 verticalAlign: "middle" 但尺寸或坐标错误，仍视为失败。
5. 长文本必须合理换行并据此调整容器高度；不得截断文字、让文字越界或为了容纳文字把字号缩到整页视图不可辨认。
6. 独立标题、说明和箭头标签可以使用独立 text，但必须对齐到所属区域并避开边框、节点、箭头尖端和其他文字。
7. 需要整体移动的卡片、插图部件或组合结构应正确分组；维护合理的前后层级，不得让背景遮住文字和连接线。

【按类型启用的设计规则】
一、流程图
1. 先列出主流程、判断、异常分支、回流、汇合与结束状态，再布局节点和箭头。主流程默认沿一条清晰视觉轴线展开，分支使用独立通道。
2. 判断使用菱形，开始和结束使用椭圆，输入、处理和提示使用矩形。每一条逻辑关系都必须对应真实 arrow，并具有有效的 startBinding 和 endBinding；“是、否、成功、失败”等文字只是标签，不能代替箭头。
3. 正常流程使用实线；回退、重试和循环使用虚线。优先使用短直线或短正交折线，箭头不得穿过无关节点或形成难以辨认的线簇。
4. 多个分支返回同一节点时，优先增加“重试、重新输入、返回上一步”等汇合节点，再用一条短线返回；禁止多条线分别绕过半张或整张画布。
5. 可恢复错误与终止错误必须在视觉上区分；多个分支汇入同一结果时使用清楚的汇合结构，不使用互相交叉的长斜线。

二、PPT／演示页
1. 每页围绕一个核心主题，建立标题、核心观点、正文和辅助信息的明确层级；先确定读者第一眼应该看到什么。
2. 根据内容选择分栏、卡片、时间轴、步骤带、数据重点或大图配文等版式。不得把一页演示稿画成大量等权小框，也不得把所有文字堆在上半页。
3. 重要结论、关键词或数据必须形成视觉焦点；同级内容对齐，标题明显高于正文。内容过多时归类或拆页，不靠缩小字号解决。
4. 不强制使用箭头；只有表达演进、方向、因果或步骤时才使用。

三、对比图
1. 先确定比较对象、共同维度和核心结论。所有对象必须按照相同维度平行比较，不得出现左右信息口径不一致。
2. 比较项少时可用对称双栏；比较项多时使用纵向成对卡片或标准表格。对应维度必须处于相同水平线或同一行，列宽、行高和内部结构保持一致。
3. 差异可以用颜色、图标或强调文字突出，但同类信息必须使用同一视觉语义；需要时在顶部或底部增加“核心差异”摘要。
4. 对比图默认不使用箭头，除非表达迁移、演进或明确方向关系。

四、表格
1. 明确表头、数据行、列数和列宽；同一列严格对齐，同一行高度一致，表头与正文有清楚的层级差异。
2. 文本根据字段语义选择左对齐、居中或右对齐；长文本自动换行并调整整行高度，不得越界、遮挡边框或与相邻单元格重叠。
3. 行列较多时优先保证可读性，可使用克制的分隔线、浅色表头或斑马纹；不得把每个单元格做成互相竞争的高饱和色块。
4. 表格整体应适配 Slide 高度和宽度，不能只缩在顶部；数据过多时拆表或拆页，不把字号缩到不可读。

五、架构图／关系图
1. 先确定层级、模块边界、依赖方向和数据流；同一层级对齐，相关模块使用容器或背景区表达分组。
2. 连接线只表达真实关系，并使用统一的颜色、线型和箭头语义；避免无法判断起点的双向线、交叉线和无意义的全连接。
3. 模块较多时优先采用分层或分区布局，不要为了装饰把所有节点围成一圈。

六、思维导图
1. 使用一个明确中心主题，一级分支围绕中心平衡展开；同一级节点使用统一样式，父子层级通过尺寸、颜色或线条区分。
2. 为各分支预留独立空间，长文本换行或扩展节点，不能互相覆盖；不得套用流程图的开始、结束和判断语义。

七、信息图
1. 先确定核心观点和阅读顺序，建立标题、关键数字／结论、图形和解释文字的视觉层级；数字与结论优先，说明次之。
2. 同一种指标使用同一种视觉编码；图标和装饰必须服务于信息，不得用无语义图形填满页面，也不得把信息图退化成普通表格或流程图。

八、插图／示意图
1. 先确定主体、背景、场景、视线焦点和必要标注；统一线宽、粗糙度、颜色和造型风格，维持合理遮挡与前后层级。
2. 人物、物体及其部件正确分组，便于继续编辑和整体移动；只有解释关系时才添加标注线或箭头。
3. 插图不要求机械填满 Slide，但主体不能无意中缩在角落。默认使用原生可编辑元素，不得把整幅结果替换为不可编辑截图，除非我明确要求插入图片。

【提交前强制校验】
写入前检查：绘图类型与用户意图是否一致；Slide 宽高比是否被用于布局；内容包围盒、宽度利用率、高度利用率、四周边距和整体视觉中心是否合理；是否有越界、非预期重叠、文字截断、文字未居中、层级混乱或过小字号。对流程图和架构图还要检查真实箭头、绑定、孤立节点、交叉、穿越和超长回流；对 PPT、对比图、表格和插图则检查各自的层级、对齐、结构与构图规则。任何一项失败都要先重新布局，不能提交。

【写入后验收】
1. 等待磁盘保存完成，再重新读取文件；确认修改前已有元素全部保留、本次元素全部存在、内存与磁盘元素一致。
2. 切换到目标 Slide，进行截图视觉验收：以完整页面缩放截图并实际查看。检查第一视觉焦点、阅读顺序、类型是否正确、文字可读性、对齐、留白、整体重心，以及是否无意中集中在上半页或某个角落。
3. 在成功取得并实际查看当前 Slide 的完整截图之前，不得声称视觉验收通过或任务已经完成。截图、切页或渲染失败时，只能报告“数据已写入，视觉验收未完成”，不能用坐标计算代替视觉验收。
4. 即使结构和几何校验通过，只要截图仍显得拥挤、松散、失衡、层级不清、关系需要猜测或不符合当前绘图类型，就必须继续调整，不能声称完成。
5. 完成报告给出：目标文件和 Slide、绘图类型、旧元素保留结果、新增／修改元素数、越界数、非预期重叠数、文字问题数、内容包围盒、宽度利用率、高度利用率、四周边距、视觉中心偏移、截图验收结果及内存与磁盘一致性；并按当前绘图类型报告专项指标，例如流程图的真实箭头与路径、表格的行列对齐、对比图的维度对应或 PPT 的信息层级。

现在请根据我接下来的自然语言需求编辑 Excalicap；不要让我重复以上安全、构图和排版规则。`;
